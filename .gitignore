/go.work.sum
/go.work
/graphics/.godot
/graphics/*.so
/graphics/*.tscn
/graphics/library.gdextension
graphics/library
graphics/save.vult
graphics/save_old.vult
releases
aviary
/graphics/musl_amd64.editor
/.claude

# Sentry musl libc++ archives (produced by build_sentry_musl.sh; not committed)
/internal/sentry/lib/

# Sentry DSN baked in at build by inject_dsn.sh (never commit the DSN)
/internal/sentry/dsn_injected.go

# Private strategy/principles (auto-loaded by Claude Code, never committed)
/CLAUDE.local.md

# graphics.gd generated bindings (regenerated by gd on every build)
graphics.gen.go
